<div class="component-title">
  <h1>{{name}}</h1>
<div class="description">
  {{description}} 
</div>
</div>
<table class="table table-bordered table-striped">
  <tbody>
    <tr>
      <td class="attribute-label">author</td>
      <td>
        {{author}}
        {{#twitter}}
          <a href="http://twitter.com/{{twitter}}" title="{{twitter}}" on Twitter"><i class="icon-twitter"></i> @{{twitter}}</a>
        {{/twitter}}
        {{#github}}
          <a href="https://github.com/{{github}}" title="{{github}}" on GitHub"><i class="icon-github"></i> @{{github}}</a>
        {{/github}}
      </td>
    </tr>
    <tr>
      <td class="attribute-label">repository</td>
      <td>
        <a href="https://github.com/{{repo}}" title="GitHub repository for {{name}}">{{repo}}</a>
      </td>
    </tr>
    <tr>
      <td class="attribute-label">type</td>
      <td>
        <span class="label">
          {{#icon}}
            <i class="icon-{{icon}}"></i>
            {{type}}
          {{/icon}}
          {{^icon}}
            <i class="icon-question"></i>
            {{type}}
          {{/icon}}
        </span>
      </td>
    </tr>
    <tr>
      <td class="attribute-label">demo</td>
      <td>
        {{#demo}}
          <a href="{{demo}}">Demo link for {{title}}</a>
        {{/demo}}
      </td>
    <tr>
      <td class="attribute-label">version</td>
      <td>{{version}}</td>
    </tr>
    <tr>
      <td class="attribute-label">keywords</td>
      <td>{{keywords}}</td>
    </tr>
    <tr>
      <td class="attribute-label">updated</td>
      <td>{{updated}}</td>
    </tr>
    <tr>
      <td class="attribute-label">dependencies</td>
      <td>
        {{#dependencies}}
        <a href="
          {{#isAudio}}
            #components/{{name}}
          {{/isAudio}}
          {{^isAudio}}
            http://github.com/{{name}}
          {{/isAudio}}
        " title="{{name}}">{{name}}</a>
        {{/dependencies}}
      </td>
    </tr>
    <tr>
      <td class="attribute-label">dependents</td>
      <td>
        {{#dependents}}
        <a href="
          {{#isAudio}}
            #components/{{name}}
          {{/isAudio}}
          {{^isAudio}}
            http://github.com/{{name}}
          {{/isAudio}}
        " title="{{name}}">{{name}}</a>
        {{/dependents}}
      </td>
    </tr>
    <tr>
      <td class="attribute-label">watchers</td>
      <td><i class="icon-star"></i> {{stars}}</td>
    </tr>
  </tbody>
</table>
<div class="activate-player">
  <button class="btn {{^liveDemo}}disabled {{/liveDemo}} btn-large btn-info">Demo {{name}}</button>
  {{#liveDemo}}
    <p>Trying out a component injects the component into the page and wires up the AudioContext, with the ability to control the component's parameters. Be aware that this may have unintended side effects.</p>
  {{/liveDemo}}
  {{^liveDemo}}
    <p>This component either lacks a type or is not a supported type component for live demo. Currently, only effect types can be demonstrated within the browser. Check out the <a href="https://github.com/{{repo}}">{{name}} repository</a> and try it out!</p>
  {{/liveDemo}}
</div>
